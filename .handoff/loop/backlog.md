# weave-loop backlog

Seeded from `TASKS.md` M1/M3, reordered to surface the item with an existing planner plan
and the open gaps the user flagged.

## Active / high-priority
- [x] WL-001: Workspace split — carve `weave-core`, `weave-inject`, `weave-mcp`, `weave` bin (TASKS.md M3; ROADMAP-v0.2 Phase 1). Merged via PR #30 (sha 82ea6dd).
- [x] WL-002 Phase A: presence daemon store + CLI — merged via PR #32.
- [x] WL-002 Phase B: MCP daemon tools (`weave_daemon_start`/`stop`/`status`) — merged via PR #33.
- [x] WL-003: zellij pane targeting — capture `ZELLIJ_PANE_ID` at registration, pass `--pane-id` to `write-chars`/`write` so injection hits the correct pane instead of the focused one (TASKS.md M1). Merged via PR #50.
- [x] WL-004: Integration tests for daemon lifecycle — env-configurable heartbeat/evict intervals; idempotency + stale-pidfile coverage. Merged via PR #50.
- [x] WL-005: Harden / execute `ralph-weave.sh` unified loop in anger — fixed broken guardian default, added gh pre-flight, stale-report scrubbing, working-tree sanity check, WEAVE_SKIP_GUARDIAN escape hatch. Merged via PR #50.
- [x] WL-006: `weave setup` — auto-register MCP server + write Claude hooks, merging with existing hooks (TASKS.md M1). Implementation verified in `setup.rs`; merged via PR #50.
- [x] WL-007: Bracketed-paste hardening for tmux — close paste mode with hex `ESC[201~` instead of bare Enter (TASKS.md M1). Implementation verified in `weave-inject/src/inject.rs`; merged via PR #50.
- [x] WL-008: Validate live injection on the zellij target box (TASKS.md M1). **Validated:** live injection works; `connect` → Live, `inject`/`notify`/`send` → `injected/ok` delivery trace. **Bug found & fixed:** zellij `list-sessions` emits ANSI color codes by default, causing `id_present` token match to fail → liveness probe falsely reports absent. Fixed by adding `--no-formatting` to the liveness probe argv. Also verified `WEAVE_MUX_DIR` is required on Nix systems where zellij lives in `/nix/store/.../toolbin` (outside `trusted_dirs()`).
- [x] WL-009: Wizard integration — build `weave` in RTX-5090 image, run `weave setup` (TASKS.md M1). **Validated:** built weave 0.2.0 in release on the live RTX-5090 box (2x NVIDIA GeForce RTX 5090, 32GB each, CUDA 13.2, Threadripper PRO 7965WX, 498GB RAM). Ran `weave setup` — MCP registered (✓ Connected), hooks wired (session/prompt/stop/wake), settings.json updated. Live injection verified: `notify` → `transport_delivered` → `injected/ok` delivery trace.

## M1 — Make it real on the box
- [x] WL-010: Decide retirement of `mcp-broker` / `repowire` (TASKS.md M1). Decision recorded in ARCHITECTURE.md §8.

## M3 — Robustness & reach
- [x] WL-011: Optional `weaved` presence daemon — online/offline, lifecycle eviction (TASKS.md M3). **Duplicate:** fully implemented in WL-002 Phase A/B (heartbeat + evict + liveness + MCP tools). No additional work required.
- [x] WL-012: More mux adapters — kitty (`kitten @ send-text`), wezterm (`wezterm cli send-text`), GNU screen (`screen -X stuff`) (TASKS.md M3). **Duplicate:** fully implemented in inject.rs with detect_target, commands_for, liveness probes, id validation, and unit tests for each backend.
- [x] WL-013: Config file — `~/.config/weave/config.toml` with default identity, nudge template, mux preference (TASKS.md M3). `mux_preference` added to `Config`; `detect_target` honors it across CLI, hooks, and MCP.

## Gaps discovered from repowire cross-reference (2026-06-07)
- [x] WL-014: Reminder injection for open asks — unacked asks resurface as a content-free nudge at the start of every subsequent prompt on the recipient side (repowire parity).
- [x] WL-015: Structured question types — extend `ask` to support choice, free-text, and tool-permission envelopes; render as actionable prompts in the recipient's pane (repowire parity). Schema + Store trait + both backends + CLI + MCP call sites done. kind/options columns added; AskKind enum with FreeText/Choice/ToolPermission.
- [x] WL-016: Scheduler / cron for messages — one-shot and recurring scheduled deliveries (`@daily`, `@hourly`, etc.) with SQLite-backed persistence and drift-safe execution (repowire parity). Schema + Store trait + both backends + CLI + MCP + tick mechanism (prompt hook + explicit `weave tick`) + tests. 442 passed (sqlite), 412 passed + 1 ignored (libsql).
- [x] WL-017: Mesh memory system — filesystem-backed scoped memory under `~/.config/weave/memory/` (global, project, persona, orchestrator) with CLI read/write/search and automatic context prefixing on ask delivery (repowire parity). Core memory.rs module + CLI + MCP tools + context prefixing on ask/send/reply/answer. 463 passed (sqlite), 433 passed + 1 ignored (libsql).
- [x] WL-018: Birth certificates / runtime identity envelopes — mint unguessable nonces at `SessionStart` to prevent path-based identity takeover during lazy MCP registration (repowire parity). birth_cert column + getrandom nonce + cert verification on re-register + backward-compat + CLI --cert + WEAVE_BIRTH_CERT env + MCP tools. 463 passed (sqlite), 433 passed + 1 ignored (libsql).
- [x] WL-019: Co-orchestrator support — allow multiple live orchestrators to coexist in the same circle for resilience against rate limits or credit caps (repowire parity). Non-force claims additive, force claims still steal. orchestrator_status returns all live holders. 463 passed (sqlite), 433 passed + 1 ignored (libsql).
- [x] WL-020: GitHub review queue integration — track PR review state across peers (`review_queue`, `mark_reviewed(pr_url)`) via CLI and MCP tools (repowire parity).
- [x] WL-021: PreToolUse tool approval — gate mutating tools (Bash, Edit, Write) behind blocking approval questions from human surfaces/peers, denying by default on timeout (repowire parity).
- [x] WL-022: Streamable-HTTP MCP transport — localhost-only opt-in endpoint for remote agents, bearer-authenticated, dangerous tools disabled by default (ROADMAP-v0.3 §4; repowire parity).
- [x] WL-023: iTerm2 injector backend — native injection support for iTerm2 terminal multiplexer (ROADMAP-v0.3 §6). AppleScript via osascript; no liveness probe (fail-open).
- [x] WL-024: Reservation leases — lightweight advisory file locks between agents to coordinate exclusive access to shared resources (ROADMAP-v0.3 §5). `leases` table + `reserve_lease`/`release_lease`/`list_leases` Store methods, both backends, CLI `weave lease {reserve,release,list}`, 3 MCP tools, integration tests. 494 passed (sqlite), 462 passed + 1 ignored (libsql).
- [x] WL-025: Stop-boundary wake — blocking `Stop`/`SubagentStop` hook that returns `additionalContext` to drive the next turn without polling (ROADMAP-v0.3 §1). `weave hook stop --wake` drains inbox with mark_read=true and emits `{"decision":"block","reason":...}` JSON when unread messages exist; `WEAVE_STOP_WAKE=1` env var also enables it. Default `stop` behaviour remains peek-only.
- [x] WL-026: Idempotency keys & trace IDs — per-message idempotency keys and distributed trace IDs for end-to-end debugging across stores (ROADMAP-v0.3 §3). 507 passed (sqlite), 467 passed + 1 ignored (libsql).
- [x] WL-027: Broadcast notify / broadcast ask — fan-out notifications and asks to all online peers in the caller's circle, not just `--to all` store broadcast (repowire parity). `weave_broadcast_notify` + `weave_broadcast_ask` MCP tools + `weave broadcast-notify` + `weave broadcast-ask` CLI. 509 passed (sqlite), 469 passed + 1 ignored (libsql).
- [x] **FrankenNetworkX crate extraction** (bonus, not in original backlog) — `fnx-classes` + `fnx-algorithms` + `fnx-runtime` wired in via Cargo git dependencies. `weave graph` command builds peer/message communication graph and runs connected_components + degree_centrality + density. 510 passed (sqlite), 470 passed + 1 ignored (libsql).

## Gaps from mcp_agent_mail cross-reference (2026-06-07)
- [x] WL-028: FTS5 full-text search on messages, threads, and subjects (mcp_agent_mail parity). `Store::search` with FTS5 virtual table (sqlite) + LIKE fallback (libsql). `weave search` CLI + `weave_search` MCP tool. 512 passed (sqlite), 472 passed + 1 ignored (libsql).
- [x] WL-029: Advisory file leases with TTL expiry and conflict detection (mcp_agent_mail parity). `lease_path_normalize` + `lease_path_conflicts` for prefix-based path conflict detection. Same-holder re-reserve extends TTL. Auto-sweep before list/reserve. `weave lease sweep` CLI + `weave_lease_sweep` MCP. 517 passed (sqlite), 477 passed + 1 ignored (libsql).
- [x] WL-030: Pre-commit Git hook for file reservation guard (mcp_agent_mail parity). `weave lease guard` checks staged files against active leases. `weave setup --git-hooks` installs idempotent pre-commit hook. 519 passed (sqlite), 479 passed + 1 ignored (libsql).
- [x] WL-031: Message importance / priority levels with urgent filtering (mcp_agent_mail parity). `MessagePriority` enum; `--priority` on `weave send`/`notify`/`broadcast-notify`; priority field on MCP `weave_send`/`weave_notify`/`weave_broadcast_notify`; cross-store priority carried through `Intent`/`outbox` and applied on pull; `weave_set_message_priority` MCP tool. 528 passed (sqlite), 488 passed + 1 ignored (libsql).
- [x] WL-032: Per-peer contact policies (open / auto / contacts_only / block_all) with explicit request/respond handshake (mcp_agent_mail parity). `ContactPolicy` enum; `weave peer-policy` CLI; `weave_set_peer_policy` / `weave_get_peer_policy` MCP tools. 528 passed (sqlite), 488 passed + 1 ignored (libsql).
- [x] WL-033: Thread summarization via LLM integration (mcp_agent_mail parity).
- [x] WL-034: Static mailbox export — self-contained portable HTML bundle with search (mcp_agent_mail parity). **DONE.** `weave export --out <path> [--for <id>] [--limit N]` writes one self-contained, offline, XSS-safe HTML doc (inline style, messages as JSON in `<script type="application/json">` with `</`→`<\/` breakout neutralization, vanilla-JS client-side search rendering via `textContent`, `<noscript>` fallback). Pure `render_mailbox_html` + centralized `html_escape` in new `weave-core/src/export.rs` (single XSS source — dashboard now reuses it); CLI in main.rs; reuses `Store::history` (NO Store/schema/dual-backend change, NO new dep, NO standing MCP tool). Unit + integration + security test layers. Green: 590 sqlite / 550 libsql + surfaces + sign. Guardian APPROVE.
- [ ] WL-034b: **Whole-DB mailbox export** (deferred from WL-034). WL-034 exports the caller's mailbox (`Store::history` is per-identity). A full cross-identity dump would need a new dual-backend `all_messages()` Store method + raises a privacy/scope decision (exports every peer's messages). Owner call on whether parity demands it before building.

## Gaps from atm-core cross-reference (2026-06-07)
- [x] WL-035: Mailbox backup / restore (atm-core parity). **DONE.** `weave backup --out <path> [--force]` / `weave restore --in <path> [--force]`: dependency-free uncompressed USTAR archive of a consistent SQLite snapshot (`VACUUM INTO`, both backends; remote libSQL bails) + `config.toml` + Claude `settings.json` hooks + MANIFEST. Read-back-verified both ends; traversal-guarded extraction (closed allow-list `safe_entry_name`); `--force`-gated overwrite (+`.bak`). New pure `weave-core/src/archive.rs` + `weave/src/backup.rs` + `Store::snapshot_to`. ZERO new deps (hand-rolled tar). Also folded the /verify GAP-2 export-write error-context fix. Guardian APPROVE.
- [x] WL-036: Post-send hooks (atm-core parity). **DONE.** Config `[[post_send_hook]]` runs an operator-authored external program on send/notify/ack. **No-shell argv-only** spawn (`argv[0]` trusted-dir-constrained); message fields reach the child ONLY as `WEAVE_HOOK_*` env vars (body never exported); pure `*`/exact/BROADCAST matcher; caps + timeout-bounded + fault-isolated (failures→stderr, never break send). Shared `weave-inject::fire_post_send_hooks` fired from CLI + MCP send/notify/ack. No Store/schema change, no new dep, no new standing MCP tool. Guardian: no-shell spawn "airtight."
- [x] WL-037: Message supersede / successor chains (atm-core parity). **DONE.** `weave send --supersedes <id>` + `weave_send` catalog property (zero standing-token cost). Additive nullable `messages.superseded_by` column (both backends, guarded migration); `Store::supersede` post-stamp with **sender-only authz** (rejects cross-identity censorship). Read semantics: hidden from unread/nudge, retained+flagged in history/thread/search/export. libsql positional projection (index 10) proven aligned. Guardian APPROVE. Gate: 626 sqlite / 581 libsql + surfaces + sign + libsql·sign, all green; +27 tests.
- [x] WL-038: Ephemeral messages with TTL and auto-sweep (atm-core parity). **DONE.** `weave send --ttl <secs>` + `weave_send` catalog `ttl` (zero standing-token). Additive nullable `messages.expires_at` (absolute deadline `ts+ttl`, both backends, trailing index 11); post-insert `set_message_expiry` (no `send()` signature change); `MAX_MSG_TTL_SECS=86400` cap (`ttl_valid` at CLI+MCP seams, mirrors lease cap). Delete-on-sweep: `sweep_expired_messages` + `gc()` fold-in + opportunistic pre-read sweeps + defense-in-depth `(expires_at IS NULL OR expires_at > now)` read guard. Cross-store carry via `outbox.ttl` re-stamped on pull-commit. Guardian APPROVE. +23 tests.
- [x] WL-039: Idle notification deduplication — replace older unread idle pings from same sender (atm-core parity). **DONE.** Opt-in `weave notify --dedup-idle` + `weave_notify` catalog `dedupIdle`. Additive nullable `messages.kind` (both backends, trailing index 12; `'idle'` stamped only on notify path); reuses the WL-037 `superseded_by` hide-spine via new `Store::supersede_prior_idle(sender,recipient,new_id)` scoped to **sender-only authz** + kind='idle' + same-recipient + unread + `id<>new`. HARD boundary test-proven: NEVER touches a distinct real message. No new hide mechanism, no new standing tool. Guardian APPROVE. +16 tests.

## Gaps from cross_agent_session_resumer cross-reference (2026-06-07)
- [x] WL-040: Session export/import in canonical format (JSON/IR) for portability across weave instances (casr parity). **DONE.** `weave session export --out <p> [--for <id>]` / `weave session import --in <p> [--as <id>] [--dry-run]` — versioned JSON envelope (`weave_session_export`/`schema_version` magic+validation). Pure `weave-core/src/session.rs` (serde + validation, no I/O) + `weave/src/session.rs` I/O handler (mirrors backup.rs: path-traversal guards, atomic temp+rename, read-back verify). Messages + mesh-memory **full round-trip** (import reuses `Store::send` → free id-remap + idempotency-key dedup → idempotent re-import; **no Store/schema change**). Untrusted-input field-bounding (check_ident/MAX_BODY/MAX_IMPORT_SUBJECT/MAX_IMPORT_FILE_BYTES). New `docs/FORMAT-session-export.md` contract. CLI-only (token-light). Guardian APPROVE. +22 tests.
- [x] WL-040b: **Faithful ask-thread replay on import** — **DONE**. `weave session import` now replays tracked asks + ask-many groups (no longer "N asks not imported"). New dual-backend `Store::import_ask` (out-of-order materializer, inserts an ask row directly in its exported `AskState` bypassing the lifecycle) + `Store::import_ask_group` + `Store::list_ask_groups`, mirrored in `store.rs` (named `params!`) and `store_libsql.rs` (positional, 15-col INSERT pinned to `row_to_ask`). Message-id remap rewires `question_msg_id`/`answer_msg_id` to the freshly minted local ids (resolved from `Store::send`'s dedup-returns-existing-id contract); dangling refs skipped+counted; idempotent re-import (dedup on `(asker,askee,question_msg_id)`/minted `parent_id`); `--as` remap; `--dry-run` counts asks/groups. `ExportedAsk` gained `kind`/`options`/`reply_to`/`close_note`/`parent_id` + new `ask_groups` envelope block (all additive `#[serde(default)]`, no `schema_version` bump). **ask_groups completed** (not deferred) — no WL-040c blocker. CLI-only (no new standing MCP tool), no new dependency. Tests: unit (both backends), integration (export→import→re-import ask thread + group, `--dry-run`), security (dangling skip, malformed state/options/parent_id/asker rejected). Both backends compile + green; clippy/fmt clean. Docs: CHANGELOG, FORMAT-session-export.md, ARCHITECTURE.md, REPOWIRE-PARITY.md.
- [x] WL-041: Read-back verification for destructive operations — verify config/hook rewrites before declaring success (casr parity). **DONE.** Every destructive config/hook writer now re-opens, re-parses, and asserts intended content present before returning Ok (descriptive Err on mismatch). `setup.rs`: `verify_settings_merged`/`verify_settings_pruned`/`verify_git_hook_written` (+ `foreign_commands`/`has_weave_command_for` — cleanly reused by WL-042) wired into merge_hooks/prune_hooks/install_git_precommit_hook; `backup.rs`: `verify_restored_bytes` after restore writes. Foreign-hook preservation asserted (never-clobber rule). config.rs unchanged (load-only). No Store/schema change. Guardian APPROVE. +17 tests (temp-HOME-pinned).
- [x] WL-042: Multi-provider lifecycle hook templates — Codex CLI, Gemini CLI, Aider hook scaffolding (casr parity). **DONE.** `weave setup --provider <claude|codex|gemini|aider>` (clap ValueEnum, default claude **byte-identical**, regression-tested). All four providers Rust-native + **zero new deps** (hand-templated: codex `~/.codex/config.toml` `notify` argv / gemini `~/.gemini/settings.json` hooks / aider `~/.aider.conf.yml` stanza). Reuses WL-041 read-back helpers; idempotent + never-clobber-foreign + atomic + read-back-verified; non-NotFound read aborts without writing. gemini/aider documented as **scaffold-with-caveat** (printed each run + in parity docs), not invented. Output sidecars (not build inputs) → no Rust-native drift. CLI-only (token budget untouched). Guardian APPROVE. +13 tests.

## Gaps from claude-code-router / cc-mirror cross-reference (2026-06-07)
- No direct gaps identified — these are model-routing / launcher tools orthogonal to weave's messaging mesh scope. Potential integration note: `weave setup --variant` could support per-variant MCP registration if cc-mirror is detected.

## Reconciliation + workspace follow-ups (2026-06-11)
- [ ] WL-043: **Collapse the multi-crate workspace back to a single crate** (P1 — the standing design goal). The loop's WL-001 split (`weave-core`/`weave-inject`/`weave-mcp`/`weave`) was unsanctioned structural drift — *not* required to port repowire. **Single-crate is the design**; the workspace is an interim hold. Do this **after the meta workspace is aligned**. Scope (no-downgrade): move all four crates' `src/` modules into one `src/`; rewrite ~114 cross-crate paths (`weave_core::`→`crate::`, `weave_inject::`→`crate::inject::`, `weave_mcp::`→`crate::mcp::`); merge the 4 `Cargo.toml`s into one (deps + `sqlite`/`libsql`/`sign`/`llm` features + `fnx-*` git deps + `[[bin]]`/`[[bench]]`); consolidate `weave/tests/` → `tests/`; run the full dual-backend + sign gate. Recovery/reference tags on origin: `backup/*` (11 tags) — **do not prune**. Re-sync CLAUDE.md + ARCHITECTURE.md to the single-crate layout in the same change.
- [x] WL-044: **Resolve the 5 Dependabot vulnerabilities (1 high, 1 moderate, 3 low)** on master (P1). **DONE (scoped + gated + tracked).** Investigation: `cargo audit` shows 4 `rustls-webpki 0.102.8` vulns (RUSTSEC-2026-0098/0099/0049/0104) + 2 unmaintained warnings (bincode, rustls-pemfile) — **ALL transitively under the optional `libsql` feature's remote-Turso TLS stack** (`libsql → hyper-rustls 0.25 → rustls 0.22 → rustls-webpki 0.102`). **The default shippable binary is advisory-clean** (`cargo tree -i rustls-webpki` on default features = no match — PROVEN). The patched `rustls-webpki >=0.103` needs `rustls 0.23`/`hyper-rustls 0.27`, which `libsql` (incl. `0.10.0-pre`, tested) hard-pins out (resolver rejects forcing it) — **upstream-blocked, not weave-fixable**; dropping libsql remote would be a downgrade (it's a shipped capability). Resolution: added a `cargo-deny` **CI advisory gate that didn't exist before** + `deny.toml` listing the 6 ids with rationale + removal trigger (gate fails on any NEW/unlisted advisory); documented the posture in `docs/SECURITY.md` §5 + CHANGELOG. **WL-044b filed** for the upstream libsql-TLS bump.
- [~] WL-044b: **Reduce the libsql advisory surface; track the rustls-webpki upstream bump** (P2). **PARTIAL — surface trimmed; webpki residual upstream-blocked.** DONE this cycle: trimmed `libsql` to `default-features = false, features = ["core","remote","tls"]` (only what weave uses — `Builder::new_local` + `Builder::new_remote`; no embedded-replica sync), which **eliminated the `bincode` advisory (RUSTSEC-2025-0141)** + dropped tonic/tonic-web/tower-http/libsql_replication; libsql backend still 668/1, audit 6→5 advisories, pruned the bincode ignore from `deny.toml`. RESIDUAL (still tracked here): the 4 `rustls-webpki 0.102.8` vulns + `rustls-pemfile` (RUSTSEC-2025-0134) live in the `tls` feature weave needs for remote HTTPS; `libsql` pins `hyper-rustls 0.25` **even on git `main`** (verified) → genuinely upstream-blocked. When `libsql` ships a `rustls 0.23`/`hyper-rustls 0.27` line, bump the dep, re-run `cargo deny check advisories`, and delete the now-`advisory-not-detected` ids from `deny.toml`. No weave src change expected.
- [x] WL-045: **Refresh README "Status"** (P2). **DONE.** Replaced the stale `v0.1.0 — 38 tests … to be confirmed` block with v0.2.0 reality: four-crate workspace linking one binary, 706 sqlite / 657 libsql green under clippy `-D warnings` + fmt, token-light single-meta-tool MCP surface (ADR-0003), live tmux/zellij injection validated end-to-end, default-OFF zero-added-dep features (libsql/sign/llm/surfaces/obscura). CHANGELOG updated.

## North-star correction — full repowire-superset + obscura (owner, 2026-06-13)
The capsule + TASK-0001 restate the mission: weave is the DEFINITIVE Rust-native SUPERSET of
repowire (MORE features, not less), in one dependency-light binary, Python-free. Most of the
orchestration mesh is already built (70 weave_* tools). These are the remaining mission gaps.
- [x] WL-046: **Full repowire feature-parity audit** (P1) — map weave's 70 `weave_*` tools against repowire's inventory (`.handoff/loop/_done/_workspace_prev/references/.../repowire.md`); produce a superset matrix (have / superset / gap) so "more than repowire, not less" is provable, not asserted. **Done:** `docs/REPOWIRE-PARITY.md` — 30/36 repowire features HAVE/SUPERSET, 4 tracked gaps (WL-047 spawn/kill, WL-048 human surfaces, + 2 minor), 2 superseded by no-daemon, 13 weave-only extras. Confirms the superset claim; feeds TASK-0001's doc restatement (now cross-linked from PRD §8 + ARCHITECTURE §0).
- [x] WL-047: **Agent spawn/kill** (P1) — Rust-native `weave_spawn_peer` / `weave_kill_peer`: spawn a new agent into a mux pane/window and kill a peer's pane/session, argv-only (no shell), per-mux (tmux/zellij/…), with birth-certificate identity (reuse the existing session nonce). repowire-parity feature weave currently lacks. Add the matching MCP + injector + test layers. **Done:** `weave_spawn_peer`/`weave_kill_peer` MCP tools + `weave spawn`/`weave kill` CLI + pure per-mux `spawn_commands`/`kill_commands` injector fns (argv-only, fail-open for zellij/screen/iterm2); two-layer spawn gate (trusted `argv[0]` + cwd allowlist `spawn_allowed_dirs`/`WEAVE_SPAWN_DIRS`, deny-by-default on MCP); parent-minted birth cert threaded via env (additive backward-compat `register_peer_full` change, both backends); injector + CLI + MCP + security test layers; green on sqlite + libSQL.
- [x] WL-048: **Rust-native human surfaces** (P1, owner-confirmed in scope) — bring back repowire's dashboard / Telegram / Slack surfaces, but Rust-native (NO Next.js/Python). **Done (ADR-0004):** read-only web dashboard (`weave dashboard`) as server-rendered HTML + SSE over the existing `weave-mcp/http.rs` (new pure `dashboard.rs`: `html_escape`/`render_dashboard`/`sse_event`/`route`; `serve_dashboard` spawns a `std::thread` per connection so SSE can't starve the MCP port); Telegram (`weave telegram`) + Slack (`weave slack`) poll-only bridges (`telegram.rs`/`slack.rs`, pure payload/parse fns + blocking reqwest loops). All behind `--features surfaces` (default OFF ⇒ zero new compiled deps; reqwest shared with `llm` ⇒ one copy). XSS-escaped, localhost-bound, bearer-gated; CLI subcommands not MCP tools (ADR-0003 token-light). No `Store` change; green on sqlite + libSQL. Closes REPOWIRE-PARITY §6.
- [x] WL-049: **obscura web-access integration** (P1) — implemented ADR-0002 (promoted proposed→accepted). **Done:** behind a default-OFF `--features obscura`, weave is the **governance plane** for obscura's stealth web access — it spawns the separate `obscura mcp` binary (argv-only `std::process::Command`, trusted-path resolved, no shell, no V8/tokio linked) and acts as a hand-rolled MCP **client** over the child's newline-delimited JSON-RPC stdio (std + serde_json only, ZERO new deps — default `cargo tree` byte-identical). ONE token-light `weave_web {action,args,describe?}` MCP dispatcher + `weave web <op>` CLI proxy all 35 `browser_*` ops (ADR-0003, not 35 eager tools); added to `DANGEROUS_TOOLS`. Pure `weave-core::webpolicy` makes the **deny-by-default** allow decision + an **SSRF/loopback URL validator** (default-deny localhost/link-local/RFC1918/`*.local`/bare-IP). Governance reuses the EXISTING permission/lease/job Store methods (`ask`/`permission_verdict`, `reserve_lease`/`release_lease`, `create_job`/`update_job`) — **no new Store method, no schema change, dual-backend unaffected**. Child lifecycle: lazy spawn, reuse, `Drop`+`weave web --stop` reap (no zombies); child stderr/tokens NEVER leaked into weave logs. webpolicy/SSRF + client-framing + governance + fake-`obscura` integration + security test layers; green on sqlite, libSQL, and `sign`+obscura. ADR-0002 web-research items (a) MCP s2s/SSRF closed by search, (b) CDP stealth/ToS documented as residual risk.

## Token-light / multi-surface — ADR-0003 (owner, 2026-06-13: full adoption, no half measures)
Owner challenge: "MCP is really a token suck." Resolution (ADR-0003): keep ALL features on ALL
surfaces; kill MCP's standing tool-table tax by architecture (progressive disclosure), not by
amputation. `token-light` is now a first-class invariant (peer of dependency-light).
- [x] WL-050: **MCP progressive-disclosure refactor** (P1, ADR-0003) — **DONE (#82).** `weave` meta-tool (`search`/`describe`/`call`/`list`) is the standing surface; `tool_catalog()` holds all 73 ops; eager-flat fallback via `WEAVE_MCP_EAGER=1`. ~73 flat tools → 1 standing tool, zero capability loss.
- [x] WL-051: **Token-light invariant + budget gate** (P1, ADR-0003) — **DONE (#84).** `token-light` added to CLAUDE.md invariants; CI-enforced `MAX_STANDING_TOOLS_BYTES` budget via `standing_mcp_surface_is_within_token_budget`. CLI parity = zero-standing-cost path.
- [~] WL-052: **Full multi-surface parity** (P1, ADR-0003) — **Foundation DONE:** `docs/MULTI-SURFACE-PARITY.md` audits every capability × surface; **CLI + MCP are at full parity**; dashboard (read-only) + bots (relay) are the WL-048 v1 baseline. Remaining write-parity decomposed below.
  - [x] WL-052a: **Dashboard write/read completeness** — **DONE (#88).** Bearer-gated POST `/api` action surface over `weave-mcp/http.rs` (distinct from the JSON-RPC POST path) reusing the same `tool_*` handlers (single source of truth); auth/caps/no-shell preserved.
  - [ ] WL-052b: **Bot command grammar** — structured `/inbox`/`/ask`/`/peers` commands on Telegram/Slack mapping to the same handlers, keeping identity-sanitization + secret-free logging. Bots currently relay free text only.

## Found by /verify (2026-06-13)
- [x] WL-053: **Capture the mux socket in the peer target (tmux `-L`/`-S`)** (P2) — **DONE (#83).** `$TMUX` socket captured at registration (persisted on the existing `peers.socket` column — no schema change) and threaded as `tmux -S <socket>` through inject/spawn/kill/liveness; socket-less peers keep the historical default-server argv.

## Found by independent broker/repowire→weave absorption audit (2026-06-17)
- [x] WL-054: **Parity-doc accuracy corrections** — corrected REPOWIRE-PARITY.md (PreToolUse advisory-not-enforcing, hosted-relay push = non-goal gap not superseded, FTS5 MATCH not LIKE, recomputed §0 tally, 3 unwired runtimes) per independent broker/repowire→weave absorption audit (2026-06-17). Broker absorption verified COMPLETE; repowire substantially complete.
- [x] WL-055: **Enforcing PreToolUse approval hook** (P1, security) — **DONE.** Closes the WL-021 gap: weave had the approval *primitive* but installed NO PreToolUse hook, so nothing actually blocked a tool. Added the `weave hook pretooluse` CLI drain (`main.rs::handle_pretooluse_hook`/`pretooluse_decision`): tolerant stdin parse (malformed/missing tool_name ⇒ defer, fail-open), `pretooluse_is_dangerous` (reuses `weave_mcp::mcp::is_dangerous_tool` + Claude-native `Bash/Edit/Write/MultiEdit/NotebookEdit`), and for a dangerous tool raises a blocking ToolPermission ask on the EXISTING `store.ask`/`permission_verdict` machinery — **deny-by-default**: approve ⇒ `allow`, deny/timeout/no-approver/broadcast-approver ⇒ `deny`, with weave's OWN short internal timeout (config `pretooluse_timeout_secs`, default 30s, clamped [1,300]) so it never relies on Claude's fail-open 600s. Pure decision JSON to stdout. Opt-in wiring `weave setup --pretooluse` (Claude only, matcher `Bash|Edit|Write`, `setup.rs::merge_pretooluse_hook_at`) with the WL-041 never-clobber-foreign + atomic + read-back-verify discipline; `uninstall` prunes it (`is_weave_command` extended for `pretooluse`); default setup does NOT install it. Config `pretooluse_approver`/`pretooluse_timeout_secs` (+ `WEAVE_PRETOOLUSE_*` env, template). **No new standing MCP tool** (budget test still green). Tests: drain logic (deny-on-timeout/allow-on-approve/deny-on-explicit-deny/safe-defer/malformed-fail-open/no-approver-deny) + black-box `weave hook pretooluse` integration tests on BOTH backends + setup matcher/read-back/foreign-preservation/uninstall-prune/default-no-write + config resolve/clamp. GREEN both backends + sign + fmt + clippy (default/libsql/sign).
- [x] WL-056: **Cross-machine PUSH delivery** (P1) — DONE. ADR-0005 accepted (2026-06-17). A→B push without B polling, daemon-free, owner-only-writes preserved. Receive = `mcp::tool_push` `weave_push` action on the existing bearer-gated `dashboard --write` POST /api surface, committing via the EXISTING `store::commit_pulled` pipeline (re-validate + verify-on-commit + Store::send + idempotency_key dedup) then the existing caller-side consent nudge into B's own pane. Send = `weave push --to <name> --host <url:port>` CLI verb (explicit-host-only, SSRF-safe), signs via `sign_intent_if_keyed`, POSTs over the existing reqwest blocking+rustls client (no new dep). Bind opt-in: `--bind <addr>` on `serve`/`dashboard` (default 127.0.0.1), fail-closed on routable-bind-without-token (checked before socket open). No new standing MCP tool (budget test green), no schema change, no new crate, default build byte-identical (`cargo tree` unchanged). Tests: `weave/tests/integration.rs` `surfaces_push` (happy-path, idempotency, bearer-missing, signed+forged, bind fail-closed) green on sqlite AND `--no-default-features --features "libsql surfaces sign"`; `is_loopback_bind` unit test in `weave-mcp/src/http.rs`. Docs: ARCHITECTURE.md §8 + §10, docs/REPOWIRE-PARITY.md §8, CHANGELOG [Unreleased].
- [x] WL-057: **weave setup persists ephemeral exe path** (P2, fixes #107) — `weave setup` wrote `std::env::current_exe()` verbatim into the GLOBAL host config (MCP + hooks), so a `…/target/{debug,release}/weave` or `…/.worktrees/…` build path dangled once removed. Added pure `setup::resolve_setup_exe` (+ `is_ephemeral_exe`) picking a stable path — explicit `--exe` > non-ephemeral current_exe (byte-identical default) > `~/.cargo/bin/weave` > non-ephemeral `weave` on `$PATH` > current_exe+warning — wired in `main.rs` (dependency-free `$PATH` scan, `--exe` existence-validated), new `weave setup --exe <PATH>` override; serve self-spawn untouched. Unit tests (ephemeral detection, all resolver branches, byte-identical-default regression, clap parse) GREEN both backends + fmt + clippy.

## Found by /verify CI triage (2026-06-17)
- [x] WL-058: **Fix the `free_port` TOCTOU dashboard-spawn flake** (P2) — `spawn_dashboard`/`spawn_dashboard_write` used `free_port()` (bind `:0` → read port → drop listener), then a readiness check `TcpStream::connect(port)` that only proved *something* was listening. Under full-suite parallel `cargo test`, a concurrent test could win the same reused port; the loser child failed to bind and exited, yet the connect check succeeded against the *other* server → the helper returned a port it didn't own → `surfaces_dashboard::dashboard_readonly_rejects_post` got a foreign (write-server) 200 instead of 403. Fix: shared race-robust `spawn_dashboard_inner` that treats **child-exited-before-listening** as the bind-collision signal and retries with a fresh port (≤8 attempts); readiness now requires our child *alive* AND the port accepting (only one process can hold a port ⇒ it's ours). Test-only (`weave/tests/integration.rs`); no production change. Stress-verified 3× green. (The separate `pretooluse_dangerous_allows_on_approve_blackbox` parallel-load flake is NOT a port race — tracked for stderr-driven root-cause, not guessed.)

## Found by live-session orchestration audit (2026-06-23)

These items come from the post-WL-058 live-session reliability loop and a fresh repo
audit on 2026-06-23. Evidence checked: current source (`main.rs`, `mcp.rs`,
`store*.rs`, `telegram.rs`, `slack.rs`), current docs/backlog, `rtk cargo
clippy --all-targets -- -D warnings`, `rtk cargo test --all-targets` (765
passed), `rtk cargo audit` (known libsql TLS advisories), and live
`weave doctor --json` / `weave scan --json` against the default store.

Follow-up verification on 2026-06-23 confirmed the deep-sweep findings are all
represented below: doctor/scan truth (WL-068), ambiguous-target prevention
(WL-069), responder non-disruptive parity (WL-070), MCP session-id/job-delegate
parity (WL-071), worker/runner dispatch (WL-072), Slack/human-surface parity
(WL-073), full-export privacy (WL-074), supply-chain reproducibility (WL-075),
backlog freshness (WL-076), workspace-vs-single-crate decision (WL-077), CC
Switch/provider policy integration (WL-078), CLI-vs-MCP/daemon friction
(WL-079), and architecture graph freshness (WL-080). Live CC Switch probing also
showed `~/.cc-switch/cc-switch.db` may be absent/unopenable in some operator
environments; WL-078 must treat DB presence/readability as an explicit diagnostic
state rather than a hard-to-interpret open failure.

- [x] WL-059: **Delivery trace truthfulness for sends/asks/answers** (P1) — **DONE (#115).** Point-to-point `send`, `ask`, `ask-many`, pretool permission asks, and tracked `answer` now append delivery trace rows consistently (`queued`, `injected`, `inject_failed`, `not_injectable`; read drains add `drained`; ask lifecycle adds `answered` through the ask state). The printed `transport_delivered`/queued/not-injectable verdicts are derived from the same trace-writing path, so the CLI cannot claim live delivery while `weave delivery --id` is empty. Added `answer` as a `delivery_log.ref_kind` and preserved ask/answer kind through hook drain. Tests cover trace rows and the transport-verdict mismatch class.
- [x] WL-060: **Shared-target / misregistration diagnostics** (P1) — **DONE (#115/#117/#119).** `peers`, `scan`, and `doctor` now flag ambiguous mux targets as `misregistered` instead of hiding shared zellij/tmux targets behind stale/online counts. JSON rows include `misregistered`; `doctor --json` includes `peers_misregistered` and `peer_statuses`; human scan marks `misregistered(shared-target)`. This does not yet *prevent* unsafe routing by alias/target; see WL-069.
- [x] WL-061: **Ask status routing view** (P1) — **DONE (#116).** Added CLI `weave ask-status --id` and MCP `weave_ask_status` joining ask state, question/answer delivery traces, and receipts into a read-time lifecycle view (`opened`, `queued`, `injected`, `received`, `answered`, `acked`). This is the operator surface for near-instant routing/status feedback without forcing the target agent to stop.
- [x] WL-062: **Peer status vocabulary in doctor/scan** (P1) — **DONE (#117).** Added explicit status tokens: `responsive`, `reachable`, `registered-stale`, `non-injectable`, `misregistered`, `dead`. Human and JSON `peers`/`scan` rows surface the token; `doctor` aggregates counts. Current limitation: this is still a folded token and can mix dimensions (for example, a stale row with a reachable pane may render `reachable`); WL-068 tracks the richer dimensional model.
- [x] WL-063: **Wrong-recipient routing anomaly detection** (P1) — **DONE (#118/#119).** A tracked ask answered by anyone other than its askee fails closed, persists a `ROUTING_ANOMALY: ask for <recipient> delivered to <actual-session>` message back to the asker, and diagnostics surface anomaly counts/last timestamp. CLI and MCP answer paths both use the anomaly path; MCP answer delivery traces use `ref_kind=answer`.
- [x] WL-064: **Skill frontmatter repair** (P2) — **DONE (#119).** Fixed `.agents/skills/weave/SKILL.md` to include YAML frontmatter so Codex/OpenAI-agent skill loading no longer warns and skips the repo skill.
- [x] WL-065: **Immediate ACK responder slice** (P1) — **DONE (#120).** Added `weave responder` one-shot/loop mode that sends idempotent, non-closing `[weave-ack]` replies for open asks without marking the original question read or answering/closing it. `ask-status` surfaces the auto-ACK and routing status can reflect `received`, `busy-queued`, `delegated-to-worker`, `cannot-answer`, and `will-answer-later`. Limitation: this is a CLI worker path only; see WL-070 for hook/daemon/MCP parity.
- [x] WL-066: **Stable session ids surfaced in diagnostics** (P1) — **DONE (#121).** `peers`, `scan`, `sessions`, and `sessions --watch` JSON include a stable non-secret `session_id` plus `session_id_basis`; human output includes `[session=sess_…]`. The id is birth-cert-backed when possible, else host+pid+target or mux target fallback.
- [x] WL-067: **First orchestration-first worker path** (P1) — **DONE (#122/#123 and runner #24).** `weave job delegate` records durable queued work assigned to a worker and sends that worker a `JOB_DELEGATED <job_id>` message/nudge, keeping the main session available as an orchestrator. Point-to-point CLI `send`, `notify`, `ask`, and `job delegate` now accept stable `sess_<16-hex>` recipients and resolve them to exactly one peer before writing. `flexnetos_runner` PR #24 added the Weave-owned `WEAVE_FXRUN_AGENT` policy seam (`agent_source`, `policy_owner`) and merged to runner `main`.

- [ ] WL-068: **Doctor/scan dimensional liveness model v2** (P1). The current status token is better than stale/online, but it still folds multiple facts into one word. Live audit on 2026-06-23 showed `doctor --json` with `peers_online=0`, `peers_stale=37`, `peers_misregistered=11`, yet `peer_statuses.reachable=15`; this is explainable (pane target can be reachable while heartbeat/process liveness is stale), but it is not obvious enough for "doctor must be accurate at all times." Add per-row and aggregate fields for `registered`, `process_alive`, `process_expected`, `pane_alive`, `injectable`, `inject_probe`, `responsive_recently`, `last_heartbeat`, `last_transport_success`, `last_response`, and `stale_reason`. Make human output explain combinations such as "pane reachable but heartbeat stale" rather than collapsing them to only `reachable`. Mirror the richer view in MCP `weave_doctor`/`weave_scan`.
- [ ] WL-069: **Unsafe target avoidance / exact session routing policy** (P1). Diagnostics flag shared mux targets, but send/ask still ultimately persist to a peer alias and the injector can still attempt a target associated with an ambiguous row. Harden routing so an ambiguous live target is either refused for live injection, degraded to queue-only with an explicit `not_injectable/ambiguous_target` trace, or requires an exact `session_id` recipient. Extend delivery traces and doctor/scan to show "injection avoided because target ambiguous." This closes the gap between "flagged" and "cannot leak/cross-deliver."
- [ ] WL-070: **Responder/ACK parity and non-disruptive daemonization** (P1). `weave responder` is CLI-only and manual; MCP `weave_ask_status` does not parse/surface `[weave-ack]` auto-acks, and there is no hook/setup-managed background responder. Add: (a) MCP `weave_responder` or meta-tool action parity; (b) MCP ask-status auto-ACK parity with CLI; (c) `weave setup` option or hook integration to run a one-shot responder on prompt/notification/wake; (d) optional long-running responder worker with health/status; (e) tests proving it never marks questions read or closes asks. Goal: every ask gets near-instant ACK/status without interrupting the main agent.
- [ ] WL-071: **MCP parity for session-id recipients and job delegation** (P1). CLI point-to-point paths now accept `sess_<16-hex>`, but MCP `weave_send`, `weave_notify`, and `weave_ask` still validate `to` as a peer identity and do not resolve session ids; MCP also lacks `weave_job_delegate`, so the orchestration-first worker path is CLI-only. Add a shared session-id resolver in a lower layer (not only `main.rs`) and wire it into MCP send/notify/ask/job-delegate. Add catalog/schema docs and black-box MCP tests.
- [ ] WL-072: **Background worker lifecycle / runner dispatch integration** (P1). `job delegate` records and notifies work but does not spawn/manage workers, claim jobs automatically, stream progress, or execute via `flexnetos_runner`. Build a first real worker lifecycle: `weave worker start/status/stop` or `weave job dispatch`, worker auto-claim/update/result loop, progress messages, lease-aware execution, and optional `WEAVE_FXRUN_AGENT` / signed-job agent selection when delegating to `flexnetos_runner`. Keep ownership boundaries: Weave coordinates and records; runner executes; handoff remains durable task ledger.
- [ ] WL-073: **Slack bot command parity and human-surface docs refresh** (P2). `docs/MULTI-SURFACE-PARITY.md` says Telegram command grammar v1 is done and Slack remains; source confirms Telegram supports `/inbox`, `/peers`, `/sessions`, `/help`, while `slack.rs` still only relays text. Add the same read-only command grammar to Slack, then add explicitly gated write commands (`/send`, `/ask`, `/answer` or `/reply`) for both bots through the shared `dispatch_request` handler. Update `.handoff/loop/backlog.md` WL-052b from partial to done only after Slack + write grammar are covered.
- [ ] WL-074: **Whole-DB/export privacy decision and implementation** (P2). WL-034b remains open: current HTML export is per-identity through `Store::history`; a full cross-identity mailbox export needs a dual-backend `all_messages()`/bounded dump API and an explicit privacy/scope decision. Decide whether this is required for repowire-superset parity or should be a confirmed non-goal. If built, include redaction/identity scoping, large-DB streaming or pagination, and security tests for XSS/export scope.
- [ ] WL-075: **Supply-chain gate reproducibility and libsql TLS watch** (P2). `rtk cargo audit` still reports the known 4 `rustls-webpki 0.102.8` vulnerabilities plus unmaintained `rustls-pemfile`, all under optional libsql remote TLS and already ignored in `deny.toml`; local `cargo deny` was not installed in this environment even though CI uses `cargo-deny-action`. Add a repo-local verification affordance (`just audit`, `cargo xtask audit`, or documented `cargo install cargo-deny`/`rtk` wrapper) so contributors can reproduce the CI advisory gate locally, and keep WL-044b active until `libsql` moves to the patched rustls stack.
- [ ] WL-076: **Backlog/docs freshness gate** (P2). The source has outpaced `.handoff/loop/backlog.md` several times (WL-059..067 were merged before the backlog recorded them). Add a lightweight release-note/backlog freshness check: every PR touching CLI/MCP/user-visible behavior must either update CHANGELOG/backlog or include an explicit "no backlog/doc change" marker. This prevents live reliability work from disappearing from the planning ledger.
- [ ] WL-077: **Re-evaluate WL-043 single-crate collapse against current token-light/multi-surface reality** (P2 decision). WL-043 still says the multi-crate workspace is "unsanctioned structural drift" and should collapse to one crate. Since then, `weave-core`, `weave-inject`, and `weave-mcp` have become useful boundaries for token-light MCP, optional surfaces/obscura, and feature isolation. Before doing a large structural rewrite, write an ADR-style decision update: either reaffirm single-crate and scope a safe migration, or mark the current workspace as accepted architecture and close/replace WL-043. Do not carry a stale architecture mandate indefinitely.
- [ ] WL-078: **CC Switch provider bridge policy integration** (P1). `weave provider-switch` now handles `claude`, `codex`, and `gemini` from CC Switch's SQLite provider DB and preserves live hooks, but it remains an operator CLI island. Corrected audit source: the user-supplied `cc-switch-main.zip` is CC Switch v3.16.3 and shows a full Tauri control plane, not only a provider table. Deep follow-up must map the repo as-is: `providers`, `provider_health`, `proxy_config`, failover queue, usage/proxy logs, MCP/prompt/skill sync tables, DeepLink import, and app coverage for `claude`, `claude-desktop`, `codex`, `gemini`, `opencode`, `openclaw`, and `hermes`. Make provider/vendor/model/proxy/health state visible to Weave orchestration without turning Weave into CC Switch. Add `doctor` provider-switch diagnostics (DB present, schema/app coverage, current provider/model per app, live config agreement, hook preservation, proxy/failover health), a `provider-switch --json`/MCP read-only status surface if it can stay token-light, and runner/job integration so delegations can request a provider/model policy through Weave rather than ad-hoc env state. Also update model discovery to account for owner policy: Ollama remains until shimmy/ruvllm are proven replacements; add shimmy/ruvllm discovery as additive, not a removal.
- [ ] WL-079: **MCP friction / CLI-daemon architecture gap audit** (P1). ADR-0003 solved standing-token cost with the `weave` meta-tool, but making CLI/daemon/hook the recommended orchestration path creates its own gaps: MCP-only agents may miss new CLI-first capabilities (`job delegate`, session-id routing, responder), progressive disclosure adds a search/describe/call learning step, and daemon/hook paths can become invisible to MCP clients unless surfaced in status. The current parity docs still contain broad "CLI and MCP are at full parity" language that must be narrowed or mechanically enforced now that CLI-first orchestration slices exist. Produce a short ADR update plus tests/gates: every new CLI capability must have an explicit MCP parity decision, every daemon/hook capability must expose a read-only MCP/CLI status, and docs should say when to prefer CLI, MCP, hook, daemon, or dashboard instead of implying MCP is always the default.
- [ ] WL-080: **Terminal-native architecture graph docs** (P2). Keep `docs/ARCHITECTURE-GRAPHS.md` current with every major plane change: component graph, primary/secondary channel map, local and across-wire session flow, Obscura governance flow, CC Switch provider/vendor flow, CLI/MCP/daemon tradeoff, and orchestrator→worker→runner flow. Treat the diagrams as review scaffolding for future agent handoffs, not as replacement for source-level `ARCHITECTURE.md`.
- [x] WL-081: **Generated target-output E2E smoke matrix** (P1) — **DONE (this slice).** Added `scripts/target_smoke.py`, a stdlib-only generated-artifact smoke runner that records Cargo/toolchain/target markers, builds root `target/debug/weave` and `target/release/weave`, executes those binaries directly against isolated temp stores, and emits `target/target-smoke/target-smoke.json` (or `--output`). It covers version/backend provenance, `doctor --json`, register/send/inbox/delivery, ask/responder/answer/ask-status, job delegate/list, graph, session export/import dry-run, backup, MCP stdio meta-tool, readonly-DB and unknown-peer negatives, plus absent CC Switch DB diagnostics when `provider-switch` is compiled in. `--full` builds feature artifacts in isolated target dirs for sign/surfaces/obscura/libsql combinations; `--clean-target` safely proves Cargo recreates `target/` from cache markers. Documented in `docs/TESTING.md`; verified locally with `python3 scripts/target_smoke.py --output /tmp/weave-target-smoke.json` (debug + release pass) and `python3 scripts/target_smoke.py --full --output /tmp/weave-target-smoke-full.json` (debug/release + sign/surfaces/obscura/libsql feature artifacts pass). Original requested matrix:
  - Build from a clean/no-`target` state and prove `target/` is recreated with expected Cargo markers.
  - Record the actual Cargo-executed compiler/toolchain path, wrapper/env, backend feature set, and binary metadata for each artifact (`debug`, `release`, `sqlite`, `libsql`, `sign`, `surfaces`, `obscura`, meaningful feature combinations).
  - Smoke the generated binaries directly from `target/debug/weave` and `target/release/weave`, not only through `cargo test`: `--version`, `doctor --json` on temp stores, `setup --dry-run`/config-safe flows where available, `send`/`inbox`/`ask`/`answer`/`ack`, delivery traces, jobs, sessions, graph, backup/restore, session export/import, MCP meta-tool `list`/`describe`/`call`, and feature-gated commands when compiled.
  - Add negative/edge smokes: missing DB, readonly DB, unknown peer, non-injectable peer, ambiguous target, missing optional external binaries, disabled features, and absent CC Switch DB.
  - Emit a machine-readable smoke report (`target-smoke.json` or similar) plus a concise human summary showing which generated artifact works, which feature gates are unsupported, and which failures are expected.
  - Keep generated `target/` outputs out of git; commit only source scripts/tests/docs. If large target caches affect local velocity, document safe cleanup/rebuild commands rather than treating cache size as source truth.
