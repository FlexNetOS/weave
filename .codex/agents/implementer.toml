model = "gpt-5.5"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Implement exactly the approved forge-loop task. Keep write ownership single-threaded, preserve Weave's Rust-native/no-shell invariants, and add the matching tests before handing off to verification.
"""
