---
name: codex-rust-env
description: Maintain a Codex Rust environment with repo guidance, project config, skills, plugins, hooks, rules, custom agents, MCP, and verification automation.
---

Use the repo-local source skill at `.agents/skills/codex-rust-env/SKILL.md`
when working inside the weave repository. This plugin-packaged copy exists so
the workflow can be distributed through the repo marketplace.
